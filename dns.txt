`sudo vim /etc/hosts`

---

SSL:

- `brew install caddy`

Sample:

```
{
    admin off
}

app.dev.hisam.dev {
    reverse_proxy http://app-testing.flycast {
        header_up Host {upstream_hostport}
        header_up X-Forwarded-Proto https
    }
    tls internal
}

```

- `caddy run --config Caddyfile`
